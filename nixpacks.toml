[phases.setup]
nixPkgs = ["...", "nodejs_20"]

[phases.build]
cmds = [
  "npm ci || npm install",
  "npm run build",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]
